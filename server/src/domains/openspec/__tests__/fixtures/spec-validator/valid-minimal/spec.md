## Purpose

Authenticate users via email + password.

## Requirements

### Requirement: SignUp

The system MUST allow users to register with a unique email address.

#### Scenario: Successful registration

- **WHEN** a user submits a new email + password
- **THEN** an account is created
- **AND** a confirmation email is sent
