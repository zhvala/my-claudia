export const NOTCH_WINDOW_TIMINGS = {
  autoCollapseCheckMs: 2000,
  hoverExpandDelayMs: 180,
  openAnimationDurationMs: 340,
  closeAnimationDurationMs: 420,
  passthroughRetryDelayMs: 500,
  passthroughWatchdogMs: 1000,
} as const;
