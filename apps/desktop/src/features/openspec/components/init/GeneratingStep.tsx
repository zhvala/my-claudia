// apps/desktop/src/features/openspec/components/init/GeneratingStep.tsx
//
// Placeholder — real implementation lands in Task 28.

import React from 'react';
import type { BootstrapScan } from '../../api.js';

export function GeneratingStep({
  scan: _scan,
}: {
  scan: BootstrapScan;
}): React.ReactElement {
  return <div className="text-sm">TODO: GeneratingStep</div>;
}
